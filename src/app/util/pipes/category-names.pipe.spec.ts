import { CategoryNamesPipe } from './category-names.pipe';

describe('CategoryNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
