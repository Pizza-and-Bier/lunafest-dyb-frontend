import { WinnerTotalPipe } from './winner-total.pipe';

describe('WinnerTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new WinnerTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
